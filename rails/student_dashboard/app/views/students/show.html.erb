<h1><%= @home_dojo.branch %>'s <%= @show_student.first_name %> <%= @show_student.last_name %></h1>
<h3>Email: <%= @show_student.email %></h3>

<h2>Cohort</h2>
<table>
    <thead>
    <tr style="font-weight: bolder;">
      <td>First Name</td>
      <td>Last Name</td>
      <td>Email</td>
    </tr>
  </thead>
  <tbody>
  <% @students_by_dojo.each do |student| %>
    <tr>
      <td><%= student.first_name %></td>
      <td><%= student.last_name %></td>
      <td><%= student.email %></td>
    </tr>
  <% end %>  
  </tbody>
</table>
<a href="/dojos">Home</a>



<p>Find me in app/views/students/show.html.erb</p>
